---
title: 喜马拉雅
sidebar_position: 10
---

喜马拉雅的安卓端所使用的加密算法。

## 后缀名

目前看来有以下几种后缀名：

- `x2m`
- `x3m`

解密后的文件均为 `m4a` 音频格式。

## 加密方式

前 `1024` 个字节被打乱并与内容密钥进行 `XOR` 操作。

后续内容未加密。

`X2M` 与 `X3M` 二者使用的算法一致，不过混淆表与内容密钥不一致。

※ 若是获得的混淆表不能正确解密，你可以尝试将索引与对应的值交换后重试。
